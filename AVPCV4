<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>AV Installation Project Tracker</title>

    <!-- PWA Meta Tags -->
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <meta name="apple-mobile-web-app-title" content="AV Tracker">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#2c3e50">

    <!-- PWA Icons -->
    <link rel="apple-touch-icon" sizes="180x180" href="data:image/svg+xml;base64,">
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml;base64,">

<style>
    * {
        box-sizing: border-box;
    }
    
    body { 
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif; 
        margin: 0;
        padding: 10px; 
        background-color: #f0f2f5; 
        color: #333;
        font-size: 14px;
        line-height: 1.4;
        -webkit-text-size-adjust: 100%;
        -webkit-tap-highlight-color: transparent;
    }
    .header { 
        background: #2c3e50; 
        color: #ecf0f1; 
        padding: 15px; 
        text-align: center; 
        margin-bottom: 20px; 
        border-radius: 8px;
    }
    .status-dashboard { 
        display: grid; 
        grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); 
        gap: 15px; 
        margin: 20px 0; 
    }
    .status-card { 
        background: #fafbfc; 
        padding: 20px; 
        text-align: center; 
        border-radius: 8px; 
        box-shadow: 0 2px 5px rgba(0,0,0,0.08); 
        border: 1px solid #e1e8ed;
    }
    .status-number { 
        font-size: 32px; 
        font-weight: bold; 
        color: #2c3e50; 
    }
    .status-label { 
        font-size: 14px; 
        color: #5a6c7d; 
        margin-top: 5px; 
        line-height: 1.3;
        font-weight: normal;
    }
    
    .table-container {
        overflow-x: auto;
        background: #fafbfc;
        border-radius: 8px;
        box-shadow: 0 2px 5px rgba(0,0,0,0.08);
        border: 1px solid #e1e8ed;
    }
    
    table { 
        width: 100%; 
        border-collapse: collapse; 
        table-layout: fixed;
        min-width: 1000px;
    }
    th { 
        background: #34495e; 
        color: #ecf0f1; 
        padding: 12px 8px; 
        text-align: left; 
        font-size: 14px;
        font-weight: bold;
        white-space: nowrap;
    }
    td { 
        padding: 8px; 
        border-bottom: 1px solid #e1e8ed; 
        vertical-align: middle;
        font-size: 14px;
        color: #333;
    }
    
    /* Checkbox column specific styling */
    th:nth-child(1), td:nth-child(1) { 
        width: 60px; 
        text-align: center;
        vertical-align: middle;
    }
    
    /* Actions column for sub-task controls */
    th:nth-child(2), td:nth-child(2) { 
        width: 80px; 
        text-align: center;
    }
    
    /* Other column widths adjusted for new column */
    th:nth-child(3), td:nth-child(3) { width: 280px; }
    th:nth-child(4), td:nth-child(4) { width: 120px; }
    th:nth-child(5), td:nth-child(5) { width: 100px; }
    th:nth-child(6), td:nth-child(6) { width: 100px; }
    th:nth-child(7), td:nth-child(7) { width: 100px; }
    th:nth-child(8), td:nth-child(8) { width: auto; min-width: 180px; }
    
    /* Alternating row colors for better readability */
    tbody tr:nth-child(even) {
        background-color: #fafbfc !important;
    }
    
    tbody tr:nth-child(odd) {
        background-color: #f0f2f5 !important;
    }
    
    /* Override any conflicting styles */
    .snow-request,
    .sub-task,
    .snow-request.sub-task {
        background-color: inherit !important;
    }
    
    /* Phase headers should remain blue across all cells */
    .phase-row, .phase-row td { 
        background: #2c3e50 !important; 
        color: #ecf0f1 !important; 
        font-weight: bold; 
    }
    .phase-row td {
        text-align: left;
        vertical-align: middle;
    }
    .section-row { 
        background: #e9ecef !important; 
        font-weight: bold; 
    }
    
    /* Status colors should override alternating background when needed */
    tbody tr.completed { 
        background-color: #d1f2d1 !important; 
    }
    tbody tr.overdue { 
        background-color: #f8d7da !important; 
    }
    tbody tr.due-soon { 
        background-color: #fff3cd !important; 
    }
    
    /* Hierarchical task styling */
    .task-level-1 { 
        font-weight: normal; 
    }
    .task-level-2 { 
        font-size: 13px; 
        opacity: 0.9;
        font-style: italic;
    }
    .task-level-2 td:nth-child(3) { 
        padding-left: 25px;
    }
    .task-level-3 td:nth-child(3) { 
        padding-left: 45px;
        font-size: 12px;
    }
    .task-level-4 td:nth-child(3) { 
        padding-left: 65px;
        font-size: 12px;
    }
    
    /* Ensure checkboxes remain centered for all levels */
    .task-level-2 td:first-child,
    .task-level-3 td:first-child,
    .task-level-4 td:first-child {
        padding-left: 8px;
        text-align: center;
    }
    
    /* Parent task styling */
    .parent-task {
        font-weight: 500 !important;
    }
    
    /* Parent task completion indicator */
    .parent-completion {
        font-size: 11px;
        color: #5a6c7d;
        margin-left: 8px;
        font-weight: normal;
    }
    
    /* Expand/collapse indicators */
    .expand-indicator {
        cursor: pointer;
        user-select: none;
        margin-right: 8px;
        font-family: monospace;
        font-weight: bold;
        color: #5a6c7d;
        transition: transform 0.2s ease;
    }
    .expand-indicator:hover {
        color: #2c3e50;
    }
    .expand-indicator.expanded {
        transform: rotate(90deg);
    }
    
    /* Hidden sub-tasks */
    .sub-task-hidden {
        display: none !important;
    }
    
    input[type="checkbox"] { 
        transform: scale(1.4); 
        margin: 0 auto;
        display: block;
        width: 18px;
        height: 18px;
        cursor: pointer;
        -webkit-appearance: checkbox;
        appearance: checkbox;
    }
    
    input[type="text"], 
    input[type="date"] { 
        width: 100%; 
        padding: 6px 4px; 
        border: 1px solid #d1d9e0; 
        font-size: 14px; 
        border-radius: 4px;
        background: #fafbfc;
        color: #333;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
    }
    
    textarea { 
        width: 100%; 
        padding: 6px 4px; 
        border: 1px solid #d1d9e0; 
        font-size: 14px; 
        border-radius: 4px;
        min-height: 50px; 
        resize: vertical; 
        background: #fafbfc;
        line-height: 1.4;
        color: #333;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
    }
    
    /* Specific input sizing for narrow columns */
    td:nth-child(4) input,
    td:nth-child(5) input,
    td:nth-child(6) input,
    td:nth-child(7) input {
        font-size: 13px;
        padding: 5px 4px;
    }
    
    .btn { 
        background: #2c3e50; 
        color: #ecf0f1; 
        border: none; 
        padding: 12px 20px; 
        margin: 5px; 
        cursor: pointer; 
        border-radius: 8px; 
        transition: background 0.3s;
        font-size: 14px;
        min-height: 44px;
        touch-action: manipulation;
        -webkit-appearance: none;
    }
    .btn:hover { 
        background: #34495e; 
    }
    
    /* Small action buttons for sub-tasks */
    .btn-small {
        padding: 4px 8px;
        font-size: 11px;
        min-height: 24px;
        margin: 1px;
        border-radius: 4px;
    }
    .btn-add {
        background: #27ae60;
    }
    .btn-add:hover {
        background: #2ecc71;
    }
    .btn-remove {
        background: #e74c3c;
    }
    .btn-remove:hover {
        background: #c0392b;
    }
    
    .auto-save-indicator { 
        color: #5a6c7d; 
        font-size: 12px; 
        margin-left: 10px;
        font-style: italic;
    }
    
    .folder-path {
        font-family: 'Courier New', monospace;
        background-color: #e9ecef;
        padding: 2px 4px;
        border-radius: 3px;
        font-size: 12px;
        color: #333;
    }
    .filter-controls { 
        background: #fafbfc; 
        padding: 15px; 
        margin-bottom: 20px; 
        border-radius: 8px; 
        box-shadow: 0 2px 5px rgba(0,0,0,0.08); 
        border: 1px solid #e1e8ed;
    }
    .filter-group { 
        display: inline-block; 
        margin-right: 20px; 
        vertical-align: top;
    }
    .filter-group label { 
        font-weight: bold; 
        margin-right: 5px; 
        display: inline-block;
        min-width: 50px;
        color: #333;
        font-size: 14px;
    }
    .filter-group select {
        padding: 6px 8px;
        border: 1px solid #d1d9e0;
        border-radius: 4px;
        font-size: 14px;
        background: #fafbfc;
        color: #333;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
    }
    .alerts { 
        background: #fafbfc; 
        padding: 15px; 
        margin-bottom: 20px; 
        border-radius: 8px; 
        box-shadow: 0 2px 5px rgba(0,0,0,0.08); 
        border: 1px solid #e1e8ed;
    }
    .alert { 
        padding: 10px; 
        margin: 5px 0; 
        border-radius: 5px;
        font-size: 14px;
        color: #333;
        line-height: 1.4;
    }
    .alert-danger { 
        background: #f8d7da; 
        border-left: 4px solid #dc3545; 
    }
    .alert-warning { 
        background: #fff3cd; 
        border-left: 4px solid #ffc107; 
    }
    .hide { 
        display: none; 
    }
    .controls { 
        text-align: center; 
        margin: 20px 0; 
    }
    
    /* iOS and Mobile optimizations */
    @media (max-width: 768px) {
        body {
            padding: 5px;
        }
        
        .header {
            padding: 10px;
            margin-bottom: 15px;
        }
        
        .header h1 {
            font-size: 20px;
            margin: 0 0 10px 0;
        }
        
        .status-dashboard {
            grid-template-columns: repeat(2, 1fr);
            gap: 10px;
            margin: 15px 0;
        }
        
        .status-card {
            padding: 15px;
        }
        
        .status-number {
            font-size: 24px;
        }
        
        .status-label {
            font-size: 12px;
        }
        
        .filter-controls {
            padding: 10px;
            margin-bottom: 15px;
        }
        
        .filter-group {
            display: block;
            margin-bottom: 10px;
            margin-right: 0;
        }
        
        .filter-group label {
            display: block;
            margin-bottom: 5px;
        }
        
        .filter-group select {
            width: 100%;
            padding: 8px;
            font-size: 16px;
        }
        
        .controls {
            text-align: left;
            margin: 15px 0;
        }
        
        .btn {
            display: block;
            width: 100%;
            margin: 8px 0;
            padding: 15px 20px;
            font-size: 16px;
            min-height: 50px;
        }
        
        .table-container {
            margin: 0 -5px;
            border-radius: 0;
        }
        
        table {
            min-width: 900px;
        }
        
        th, td {
            padding: 6px 4px;
            font-size: 13px;
        }
        
        th:nth-child(1), td:nth-child(1) { 
            width: 50px; 
        }
        th:nth-child(2), td:nth-child(2) { 
            width: 70px; 
        }
        th:nth-child(3), td:nth-child(3) { 
            width: 200px; 
        }
        th:nth-child(4), td:nth-child(4) { 
            width: 90px; 
        }
        th:nth-child(5), td:nth-child(5) { 
            width: 80px; 
        }
        th:nth-child(6), td:nth-child(6) { 
            width: 80px; 
        }
        th:nth-child(7), td:nth-child(7) { 
            width: 80px; 
        }
        
        input[type="text"], 
        input[type="date"] {
            font-size: 16px;
            padding: 8px 6px;
            border-radius: 8px;
        }
        
        textarea {
            font-size: 16px;
            padding: 8px 6px;
            border-radius: 8px;
            min-height: 60px;
        }
        
        .phase-row td {
            font-size: 13px;
            padding: 8px 6px;
        }
        
        .btn-small {
            padding: 3px 6px;
            font-size: 10px;
            min-height: 20px;
        }
    }
    
    /* iOS Safari specific */
    @supports (-webkit-touch-callout: none) {
        .header {
            padding-top: max(10px, env(safe-area-inset-top));
        }
        
        body {
            padding-bottom: max(10px, env(safe-area-inset-bottom));
        }
    }
    
    /* Print styles */
    @media print {
        .controls, .filter-controls, .alerts {
            display: none;
        }
        
        .header {
            background: #2c3e50 !important;
            -webkit-print-color-adjust: exact;
        }
        
        table {
            font-size: 12px;
        }
        
        input, textarea {
            border: none;
            background: transparent;
            font-size: 11px;
            color: #333;
        }
        
        body {
            font-size: 12px;
        }
        
        .btn-small {
            display: none;
        }
    }
</style>

</head>
<body>
    <div class="header">
        <h1>AV Installation Project Tracker</h1>
        <div style="display: flex; gap: 15px; justify-content: center; align-items: center; margin-top: 15px; flex-wrap: wrap;">
            <input type="text" id="projectName" placeholder="Enter project name" style="padding: 8px; font-size: 16px; min-width: 200px; border: 1px solid #d1d9e0; border-radius: 8px; background: #fafbfc; color: #333; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;">
            <input type="text" id="projectManager" placeholder="Enter project manager" style="padding: 8px; font-size: 16px; min-width: 200px; border: 1px solid #d1d9e0; border-radius: 8px; background: #fafbfc; color: #333; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;">
            <span class="auto-save-indicator" id="autoSaveIndicator">Auto-saved</span>
        </div>
        <div style="margin-top: 12px; font-size: 12px; color: #ecf0f1; font-style: italic; text-align: center;">
            📱 <strong>iOS Users:</strong> Files save to iCloud Drive or Google Drive when accessed through Files app<br>
            💡 <strong>Desktop:</strong> Configure browser download folder to this file's location<br>
            ✨ <strong>New:</strong> Click "+" next to any task to add sub-tasks dynamically
        </div>
    </div>

    <div class="alerts" id="alertsContainer">
        <div class="alert" style="background: #e7f3ff; border-left: 4px solid #007AFF;">
            <strong>🎉 Enhanced AV Tracker Ready!</strong><br>
            • Export to Excel with hierarchical formatting<br>
            • PWA support - install as mobile app<br>
            • iOS-optimized with cloud storage integration<br>
            • Advanced filtering and progress tracking
        </div>
    </div>

    <div class="status-dashboard">
        <div class="status-card">
            <div class="status-number" id="overallPercent">0%</div>
            <div class="status-label">Overall Progress</div>
        </div>
        <div class="status-card">
            <div class="status-number" id="phase1Percent">0%</div>
            <div class="status-label">Pre-Installation Logistics</div>
        </div>
        <div class="status-card">
            <div class="status-number" id="phase2Percent">0%</div>
            <div class="status-label">Pre-Installation AV Setup</div>
        </div>
        <div class="status-card">
            <div class="status-number" id="phase3Percent">0%</div>
            <div class="status-label">Installation & Commissioning</div>
        </div>
        <div class="status-card">
            <div class="status-number" id="phase4Percent">0%</div>
            <div class="status-label">Post-Installation Commissioning</div>
        </div>
        <div class="status-card">
            <div class="status-number" id="phase5Percent">0%</div>
            <div class="status-label">Post-Installation Logistics</div>
        </div>
    </div>

    <div class="filter-controls">
        <div class="filter-group">
            <label>View:</label>
            <select id="viewFilter">
                <option value="all">All Tasks</option>
                <option value="incomplete">Incomplete Only</option>
                <option value="complete">Complete Only</option>
                <option value="overdue">Overdue</option>
                <option value="due-soon">Due This Week</option>
            </select>
        </div>
        <div class="filter-group">
            <label>Phase:</label>
            <select id="phaseFilter">
                <option value="all">All Phases</option>
                <option value="1">Phase 1</option>
                <option value="2">Phase 2</option>
                <option value="3">Phase 3</option>
                <option value="4">Phase 4</option>
                <option value="5">Phase 5</option>
            </select>
        </div>
        <div class="filter-group">
            <label>Hierarchy:</label>
            <select id="hierarchyFilter">
                <option value="expanded">Show All (Expanded)</option>
                <option value="collapsed">Show Parents Only</option>
                <option value="parents-only">Hide Sub-tasks</option>
            </select>
        </div>
    </div>

    <div class="controls">
        <button class="btn" onclick="exportToExcel()">📊 Export to Excel</button>
        <button class="btn" onclick="saveProject()">💾 Save Project</button>
        <button class="btn" onclick="loadProject()">📂 Load Project</button>
        <button class="btn" onclick="addCustomTask()">➕ Add Custom Task</button>
        <button class="btn" onclick="updateProgress()" style="background: #27ae60;">🔄 Refresh Progress</button>
        <input type="file" id="projectFileInput" accept=".json" style="display: none;" onchange="handleFileLoad(event)">
    </div>

    <div class="table-container">
        <table id="mainTable">
            <thead>
                <tr>
                    <th>Done</th>
                    <th>Actions</th>
                    <th>Task</th>
                    <th>Assigned To</th>
                    <th>Start Date</th>
                    <th>Due Date</th>
                    <th>Complete Date</th>
                    <th>Notes/Artifacts</th>
                </tr>
            </thead>
            <tbody>
                <!-- Phase 1: Pre-Installation – Logistics -->
                <tr class="phase-row">
                    <td colspan="8"><strong>PHASE 1: PRE-INSTALLATION – LOGISTICS</strong></td>
                </tr>
                
                <tr class="task-level-1" data-task-id="1001" data-phase="1">
                    <td><input type="checkbox" data-phase="1" data-task-id="1001"></td>
                    <td>
                        <button class="btn btn-small btn-add" onclick="addSubTask('1001')" title="Add Sub-task">+</button>
                    </td>
                    <td>
                        <span class="expand-indicator" onclick="toggleSubTasks('1001')">▶</span>
                        Create Project Folder in Egnyte
                        <span class="parent-completion" id="completion-1001"></span>
                    </td>
                    <td><input type="text" placeholder="Assigned to"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><textarea placeholder="Z:\Shared\It Unified Communications\AV\! Current Buildouts - create default project tracker and place/save any/all working docs into this folder"></textarea></td>
                </tr>
                
                <tr class="task-level-1" data-task-id="1002" data-phase="1">
                    <td><input type="checkbox" data-phase="1" data-task-id="1002"></td>
                    <td>
                        <button class="btn btn-small btn-add" onclick="addSubTask('1002')" title="Add Sub-task">+</button>
                    </td>
                    <td>
                        <span class="expand-indicator" onclick="toggleSubTasks('1002')">▶</span>
                        Create SNOW Ticket for Project
                        <span class="parent-completion" id="completion-1002"></span>
                    </td>
                    <td><input type="text" placeholder="Assigned to"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><textarea placeholder="SNOW ticket number (if not already created)"></textarea></td>
                </tr>
                
                <tr class="task-level-1" data-task-id="1003" data-phase="1">
                    <td><input type="checkbox" data-phase="1" data-task-id="1003"></td>
                    <td>
                        <button class="btn btn-small btn-add" onclick="addSubTask('1003')" title="Add Sub-task">+</button>
                    </td>
                    <td>
                        <span class="expand-indicator" onclick="toggleSubTasks('1003')">▶</span>
                        Initial Project Information Gathered
                        <span class="parent-completion" id="completion-1003"></span>
                    </td>
                    <td><input type="text" placeholder="Assigned to"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><textarea placeholder="What info needs to be gathered, where to get it, how to determine contacts - reference doc for requirements"></textarea></td>
                </tr>
                
                <tr class="task-level-1" data-task-id="1004" data-phase="1">
                    <td><input type="checkbox" data-phase="1" data-task-id="1004"></td>
                    <td>
                        <button class="btn btn-small btn-add" onclick="addSubTask('1004')" title="Add Sub-task">+</button>
                    </td>
                    <td>
                        <span class="expand-indicator" onclick="toggleSubTasks('1004')">▶</span>
                        RACI Gathered to Determine Stakeholders
                        <span class="parent-completion" id="completion-1004"></span>
                    </td>
                    <td><input type="text" placeholder="Assigned to"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><textarea placeholder="RACI matrix with stakeholder roles and responsibilities"></textarea></td>
                </tr>

                <!-- Example with existing sub-task to show hierarchy -->
                <tr class="task-level-1" data-task-id="1014" data-phase="1">
                    <td><input type="checkbox" data-phase="1" data-task-id="1014"></td>
                    <td>
                        <button class="btn btn-small btn-add" onclick="addSubTask('1014')" title="Add Sub-task">+</button>
                    </td>
                    <td>
                        <span class="expand-indicator expanded" onclick="toggleSubTasks('1014')" style="color: #2c3e50;">▼</span>
                        Verify All Hardware Has Shipped to AV Vendor
                        <span class="parent-completion" id="completion-1014">(1/1 - 100%)</span>
                    </td>
                    <td><input type="text" placeholder="Assigned to"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><textarea placeholder="Hardware shipment confirmed"></textarea></td>
                </tr>
                
                <tr class="task-level-2 sub-task" data-parent-id="1014" data-task-id="1014-1" data-phase="1">
                    <td><input type="checkbox" data-phase="1" data-task-id="1014-1" data-parent-id="1014" checked></td>
                    <td>
                        <button class="btn btn-small btn-remove" onclick="removeTask('1014-1')" title="Remove Sub-task">×</button>
                    </td>
                    <td>Provide Projected Arrival Date in Tracker</td>
                    <td><input type="text" placeholder="Assigned to" value="John Doe"></td>
                    <td><input type="date" value="2025-01-15"></td>
                    <td><input type="date" value="2025-01-20"></td>
                    <td><input type="date" value="2025-01-18"></td>
                    <td><textarea placeholder="Projected arrival date updated in project tracker">Arrival date confirmed for January 20th</textarea></td>
                </tr>

                <!-- Phase 2: Pre-Installation - AV Setup -->
                <tr class="phase-row">
                    <td colspan="8"><strong>PHASE 2: PRE-INSTALLATION - AV SETUP</strong></td>
                </tr>
                
                <tr class="task-level-1" data-task-id="2001" data-phase="2">
                    <td><input type="checkbox" data-phase="2" data-task-id="2001"></td>
                    <td>
                        <button class="btn btn-small btn-add" onclick="addSubTask('2001')" title="Add Sub-task">+</button>
                    </td>
                    <td>
                        <span class="expand-indicator" onclick="toggleSubTasks('2001')">▶</span>
                        Verify Space Name with Stakeholders
                        <span class="parent-completion" id="completion-2001"></span>
                    </td>
                    <td><input type="text" placeholder="Assigned to"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><textarea placeholder="Space name confirmed with stakeholders"></textarea></td>
                </tr>
                
                <tr class="task-level-1 snow-request" data-task-id="2002" data-phase="2">
                    <td><input type="checkbox" data-phase="2" data-task-id="2002"></td>
                    <td>
                        <button class="btn btn-small btn-add" onclick="addSubTask('2002')" title="Add Sub-task">+</button>
                    </td>
                    <td>
                        <span class="expand-indicator" onclick="toggleSubTasks('2002')">▶</span>
                        SNOW Request to Create/Edit Room in AD/Entra with Calendar
                        <span class="parent-completion" id="completion-2002"></span>
                    </td>
                    <td><input type="text" placeholder="Assigned to"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><textarea placeholder="SNOW request for AD/Entra room creation with calendar functionality"></textarea></td>
                </tr>

                <!-- Phase 3: Installation and Commissioning -->
                <tr class="phase-row">
                    <td colspan="8"><strong>PHASE 3: INSTALLATION AND COMMISSIONING</strong></td>
                </tr>
                
                <tr class="task-level-1" data-task-id="3001" data-phase="3">
                    <td><input type="checkbox" data-phase="3" data-task-id="3001"></td>
                    <td>
                        <button class="btn btn-small btn-add" onclick="addSubTask('3001')" title="Add Sub-task">+</button>
                    </td>
                    <td>
                        <span class="expand-indicator" onclick="toggleSubTasks('3001')">▶</span>
                        AV Vendor Delivery and Installation of Hardware
                        <span class="parent-completion" id="completion-3001"></span>
                    </td>
                    <td><input type="text" placeholder="Assigned to"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><textarea placeholder="Hardware delivery and installation - requested, scheduled, complete"></textarea></td>
                </tr>

                <!-- Phase 4: Post-Installation Commissioning -->
                <tr class="phase-row">
                    <td colspan="8"><strong>PHASE 4: POST-INSTALLATION COMMISSIONING</strong></td>
                </tr>
                
                <tr class="task-level-1" data-task-id="4001" data-phase="4">
                    <td><input type="checkbox" data-phase="4" data-task-id="4001"></td>
                    <td>
                        <button class="btn btn-small btn-add" onclick="addSubTask('4001')" title="Add Sub-task">+</button>
                    </td>
                    <td>
                        <span class="expand-indicator" onclick="toggleSubTasks('4001')">▶</span>
                        Create Room System Instructions
                        <span class="parent-completion" id="completion-4001"></span>
                    </td>
                    <td><input type="text" placeholder="Assigned to"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><textarea placeholder="Room system instructions created for end users"></textarea></td>
                </tr>

                <!-- Phase 5: Post-Installation Logistics -->
                <tr class="phase-row">
                    <td colspan="8"><strong>PHASE 5: POST-INSTALLATION LOGISTICS</strong></td>
                </tr>
                
                <tr class="task-level-1" data-task-id="5001" data-phase="5">
                    <td><input type="checkbox" data-phase="5" data-task-id="5001"></td>
                    <td>
                        <button class="btn btn-small btn-add" onclick="addSubTask('5001')" title="Add Sub-task">+</button>
                    </td>
                    <td>
                        <span class="expand-indicator" onclick="toggleSubTasks('5001')">▶</span>
                        Review and Approve Invoices for Payment
                        <span class="parent-completion" id="completion-5001"></span>
                    </td>
                    <td><input type="text" placeholder="Assigned to"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><textarea placeholder="Vendor invoices reviewed and approved for payment"></textarea></td>
                </tr>
            </tbody>
        </table>
    </div>

<script>
    // Global project data with hierarchical task tracking
    let projectData = {
        name: '',
        manager: '',
        tasks: []
    };
    
    // Task hierarchy management
    let taskHierarchy = {};
    let nextTaskId = 10000;

    // Detect iOS device
    function isIOS() {
        return /iPad|iPhone|iPod/.test(navigator.userAgent) || 
               (navigator.platform === 'MacIntel' && navigator.maxTouchPoints > 1);
    }

    // Detect if running as PWA
    function isPWA() {
        return window.matchMedia('(display-mode: standalone)').matches ||
               window.navigator.standalone === true;
    }

    // Initialize the application
    document.addEventListener('DOMContentLoaded', function() {
        console.log('Initializing Enhanced AV Tracker...');
        
        setupEventListeners();
        initializeTaskHierarchy();
        
        if (isIOS()) {
            console.log('iOS device detected, applying optimizations...');
            setupIOSOptimizations();
        }
        
        setupPWAInstaller();
        
        setTimeout(() => {
            updateProgress();
            console.log('Enhanced setup complete');
        }, 100);
    });

    // Initialize task hierarchy from existing DOM structure
    function initializeTaskHierarchy() {
        const rows = document.querySelectorAll('#mainTable tbody tr[data-task-id]');
        
        rows.forEach(row => {
            const taskId = row.getAttribute('data-task-id');
            const parentId = row.getAttribute('data-parent-id');
            const phase = row.getAttribute('data-phase');
            
            if (!taskHierarchy[taskId]) {
                taskHierarchy[taskId] = {
                    id: taskId,
                    parentId: parentId || null,
                    phase: phase,
                    children: [],
                    isExpanded: true
                };
            }
            
            if (parentId && taskHierarchy[parentId]) {
                if (!taskHierarchy[parentId].children.includes(taskId)) {
                    taskHierarchy[parentId].children.push(taskId);
                }
            }
        });
        
        console.log('Task hierarchy initialized:', taskHierarchy);
    }

    // iOS-specific setup
    function setupIOSOptimizations() {
        document.body.classList.add('ios-device');
        
        const inputs = document.querySelectorAll('input:not([type="checkbox"]), textarea, select');
        inputs.forEach(input => {
            input.addEventListener('focus', function() {
                if (isIOS()) {
                    this.style.fontSize = '16px';
                }
            });
            
            input.addEventListener('blur', function() {
                if (isIOS()) {
                    this.style.fontSize = '';
                }
            });
        });
    }

    // PWA installer
    let deferredPrompt;
    
    function setupPWAInstaller() {
        window.addEventListener('beforeinstallprompt', (e) => {
            e.preventDefault();
            deferredPrompt = e;
            showInstallButton();
        });
    }

    function showInstallButton() {
        if (!isPWA() && !isIOS()) {
            const controls = document.querySelector('.controls');
            const installBtn = document.createElement('button');
            installBtn.className = 'btn';
            installBtn.textContent = '📱 Install as App';
            installBtn.onclick = installPWA;
            controls.insertBefore(installBtn, controls.firstChild);
        }
    }

    function installPWA() {
        if (deferredPrompt) {
            deferredPrompt.prompt();
            deferredPrompt.userChoice.then((result) => {
                deferredPrompt = null;
            });
        }
    }

    // Setup event listeners
    function setupEventListeners() {
        document.getElementById('projectName').addEventListener('input', autoSave);
        document.getElementById('projectManager').addEventListener('input', autoSave);
        
        document.getElementById('viewFilter').addEventListener('change', applyFilters);
        document.getElementById('phaseFilter').addEventListener('change', applyFilters);
        document.getElementById('hierarchyFilter').addEventListener('change', applyHierarchyFilter);
        
        setupCheckboxListeners();

        const inputs = document.querySelectorAll('input, textarea');
        inputs.forEach(input => {
            if (input.type !== 'checkbox') {
                input.addEventListener('input', autoSave);
            }
        });
    }

    // Setup checkbox listeners with hierarchical support
    function setupCheckboxListeners() {
        const checkboxes = document.querySelectorAll('input[type="checkbox"]');
        checkboxes.forEach(checkbox => {
            checkbox.addEventListener('change', function(e) {
                const taskId = this.getAttribute('data-task-id');
                const parentId = this.getAttribute('data-parent-id');
                const isChecked = this.checked;
                
                if (taskHierarchy[taskId] && taskHierarchy[taskId].children.length > 0) {
                    updateChildTasks(taskId, isChecked);
                }
                
                if (parentId) {
                    updateParentTask(parentId);
                }
                
                updateProgress();
                updateTaskRow(this);
                autoSave();
                
                console.log('Checkbox changed:', {
                    taskId: taskId,
                    checked: isChecked,
                    parentId: parentId,
                    phase: this.getAttribute('data-phase')
                });
            });
        });
    }

    // Update child tasks when parent is checked/unchecked
    function updateChildTasks(parentId, isChecked) {
        if (!taskHierarchy[parentId]) return;
        
        const children = taskHierarchy[parentId].children;
        children.forEach(childId => {
            const childCheckbox = document.querySelector(`input[data-task-id="${childId}"]`);
            if (childCheckbox && childCheckbox.checked !== isChecked) {
                childCheckbox.checked = isChecked;
                updateTaskRow(childCheckbox);
                
                if (taskHierarchy[childId] && taskHierarchy[childId].children.length > 0) {
                    updateChildTasks(childId, isChecked);
                }
            }
        });
    }

    // Update parent task completion based on children
    function updateParentTask(parentId) {
        if (!taskHierarchy[parentId]) return;
        
        const children = taskHierarchy[parentId].children;
        if (children.length === 0) return;
        
        let completedChildren = 0;
        let totalChildren = children.length;
        
        children.forEach(childId => {
            const childCheckbox = document.querySelector(`input[data-task-id="${childId}"]`);
            if (childCheckbox && childCheckbox.checked) {
                completedChildren++;
            }
        });
        
        const parentCheckbox = document.querySelector(`input[data-task-id="${parentId}"]`);
        const completionPercentage = Math.round((completedChildren / totalChildren) * 100);
        
        if (parentCheckbox) {
            if (completedChildren === totalChildren) {
                parentCheckbox.checked = true;
                parentCheckbox.indeterminate = false;
            } else if (completedChildren > 0) {
                parentCheckbox.checked = false;
                parentCheckbox.indeterminate = true;
            } else {
                parentCheckbox.checked = false;
                parentCheckbox.indeterminate = false;
            }
            updateTaskRow(parentCheckbox);
        }
        
        const completionIndicator = document.getElementById(`completion-${parentId}`);
        if (completionIndicator) {
            if (totalChildren > 0) {
                completionIndicator.textContent = `(${completedChildren}/${totalChildren} - ${completionPercentage}%)`;
                completionIndicator.style.display = 'inline';
            } else {
                completionIndicator.style.display = 'none';
            }
        }
        
        const grandparentId = taskHierarchy[parentId].parentId;
        if (grandparentId) {
            updateParentTask(grandparentId);
        }
    }

    // Add sub-task to a parent task
    function addSubTask(parentId) {
        const taskName = prompt('Enter sub-task name:');
        if (!taskName || !taskName.trim()) return;
        
        const parentRow = document.querySelector(`tr[data-task-id="${parentId}"]`);
        if (!parentRow) return;
        
        const phase = parentRow.getAttribute('data-phase');
        const newTaskId = `${parentId}-${Date.now()}`;
        
        const newRow = document.createElement('tr');
        newRow.className = 'task-level-2 sub-task';
        newRow.setAttribute('data-parent-id', parentId);
        newRow.setAttribute('data-task-id', newTaskId);
        newRow.setAttribute('data-phase', phase);
        
        newRow.innerHTML = `
            <td><input type="checkbox" data-phase="${phase}" data-task-id="${newTaskId}" data-parent-id="${parentId}"></td>
            <td>
                <button class="btn btn-small btn-add" onclick="addSubTask('${newTaskId}')" title="Add Sub-task">+</button>
                <button class="btn btn-small btn-remove" onclick="removeTask('${newTaskId}')" title="Remove Sub-task">×</button>
            </td>
            <td>${taskName.trim()}</td>
            <td><input type="text" placeholder="Assigned to"></td>
            <td><input type="date"></td>
            <td><input type="date"></td>
            <td><input type="date"></td>
            <td><textarea placeholder="Notes for this sub-task"></textarea></td>
        `;
        
        let insertAfter = parentRow;
        if (taskHierarchy[parentId] && taskHierarchy[parentId].children.length > 0) {
            const children = taskHierarchy[parentId].children;
            const lastChildId = children[children.length - 1];
            const lastChildRow = document.querySelector(`tr[data-task-id="${lastChildId}"]`);
            if (lastChildRow) {
                insertAfter = lastChildRow;
            }
        }
        
        insertAfter.parentNode.insertBefore(newRow, insertAfter.nextSibling);
        
        if (!taskHierarchy[parentId]) {
            taskHierarchy[parentId] = { children: [] };
        }
        taskHierarchy[parentId].children.push(newTaskId);
        
        taskHierarchy[newTaskId] = {
            id: newTaskId,
            parentId: parentId,
            phase: phase,
            children: [],
            isExpanded: true
        };
        
        const parentExpandIndicator = parentRow.querySelector('.expand-indicator');
        if (parentExpandIndicator) {
            parentExpandIndicator.style.visibility = 'visible';
            parentRow.classList.add('parent-task');
        }
        
        setupCheckboxListeners();
        
        const newInputs = newRow.querySelectorAll('input:not([type="checkbox"]), textarea');
        newInputs.forEach(input => {
            input.addEventListener('input', autoSave);
            
            if (isIOS()) {
                input.addEventListener('focus', function() {
                    this.style.fontSize = '16px';
                });
                input.addEventListener('blur', function() {
                    this.style.fontSize = '';
                });
            }
        });
        
        updateParentTask(parentId);
        updateProgress();
        autoSave();
        
        console.log(`Added sub-task ${newTaskId} to parent ${parentId}`);
    }

    // Remove a task and its children
    function removeTask(taskId) {
        if (!confirm('Are you sure you want to remove this task and all its sub-tasks?')) {
            return;
        }
        
        const taskRow = document.querySelector(`tr[data-task-id="${taskId}"]`);
        if (!taskRow) return;
        
        const parentId = taskRow.getAttribute('data-parent-id');
        
        if (taskHierarchy[taskId] && taskHierarchy[taskId].children.length > 0) {
            const children = [...taskHierarchy[taskId].children];
            children.forEach(childId => {
                removeTask(childId);
            });
        }
        
        if (parentId && taskHierarchy[parentId]) {
            const childIndex = taskHierarchy[parentId].children.indexOf(taskId);
            if (childIndex > -1) {
                taskHierarchy[parentId].children.splice(childIndex, 1);
            }
            
            updateParentTask(parentId);
            
            if (taskHierarchy[parentId].children.length === 0) {
                const parentRow = document.querySelector(`tr[data-task-id="${parentId}"]`);
                const expandIndicator = parentRow?.querySelector('.expand-indicator');
                if (expandIndicator) {
                    expandIndicator.style.visibility = 'hidden';
                    parentRow.classList.remove('parent-task');
                }
            }
        }
        
        delete taskHierarchy[taskId];
        taskRow.remove();
        
        updateProgress();
        autoSave();
        
        console.log(`Removed task ${taskId}`);
    }

    // Toggle sub-task visibility
    function toggleSubTasks(parentId) {
        if (!taskHierarchy[parentId]) return;
        
        const children = taskHierarchy[parentId].children;
        const isExpanded = taskHierarchy[parentId].isExpanded;
        const expandIndicator = document.querySelector(`tr[data-task-id="${parentId}"] .expand-indicator`);
        
        children.forEach(childId => {
            const childRow = document.querySelector(`tr[data-task-id="${childId}"]`);
            if (childRow) {
                if (isExpanded) {
                    childRow.classList.add('sub-task-hidden');
                } else {
                    childRow.classList.remove('sub-task-hidden');
                }
            }
        });
        
        taskHierarchy[parentId].isExpanded = !isExpanded;
        
        if (expandIndicator) {
            if (taskHierarchy[parentId].isExpanded) {
                expandIndicator.textContent = '▼';
                expandIndicator.classList.add('expanded');
            } else {
                expandIndicator.textContent = '▶';
                expandIndicator.classList.remove('expanded');
            }
        }
    }

    // Apply hierarchy filter
    function applyHierarchyFilter() {
        const hierarchyFilter = document.getElementById('hierarchyFilter').value;
        const allRows = document.querySelectorAll('#mainTable tbody tr[data-task-id]');
        
        allRows.forEach(row => {
            const taskId = row.getAttribute('data-task-id');
            const parentId = row.getAttribute('data-parent-id');
            
            if (hierarchyFilter === 'collapsed') {
                if (parentId) {
                    row.classList.add('sub-task-hidden');
                } else {
                    row.classList.remove('sub-task-hidden');
                    const expandIndicator = row.querySelector('.expand-indicator');
                    if (expandIndicator && taskHierarchy[taskId]) {
                        taskHierarchy[taskId].isExpanded = false;
                        expandIndicator.textContent = '▶';
                        expandIndicator.classList.remove('expanded');
                    }
                }
            } else if (hierarchyFilter === 'parents-only') {
                if (parentId) {
                    row.style.display = 'none';
                } else {
                    row.style.display = '';
                }
            } else {
                row.classList.remove('sub-task-hidden');
                row.style.display = '';
                const expandIndicator = row.querySelector('.expand-indicator');
                if (expandIndicator && taskHierarchy[taskId]) {
                    taskHierarchy[taskId].isExpanded = true;
                    expandIndicator.textContent = '▼';
                    expandIndicator.classList.add('expanded');
                }
            }
        });
        
        applyFilters();
    }

    // Enhanced progress calculation with hierarchy
    function updateProgress() {
        const phases = [1, 2, 3, 4, 5];
        let overallTotal = 0;
        let overallCompleted = 0;
        
        console.log('Updating hierarchical progress...');

        phases.forEach(phase => {
            let phaseTotal = 0;
            let phaseCompleted = 0;
            
            const phaseTasks = document.querySelectorAll(`input[type="checkbox"][data-phase="${phase}"]`);
            
            phaseTasks.forEach(checkbox => {
                const taskId = checkbox.getAttribute('data-task-id');
                const parentId = checkbox.getAttribute('data-parent-id');
                
                if (!parentId) {
                    const taskWeight = calculateTaskWeight(taskId);
                    phaseTotal += taskWeight;
                    
                    if (checkbox.checked || checkbox.indeterminate) {
                        phaseCompleted += calculateTaskCompletion(taskId) * taskWeight;
                    }
                }
            });
            
            const percentage = phaseTotal > 0 ? Math.round((phaseCompleted / phaseTotal) * 100) : 0;
            const phaseElement = document.getElementById(`phase${phase}Percent`);
            
            if (phaseElement) {
                phaseElement.textContent = `${percentage}%`;
            }
            
            console.log(`Phase ${phase}: ${phaseCompleted.toFixed(2)}/${phaseTotal} (${percentage}%)`);
            
            overallTotal += phaseTotal;
            overallCompleted += phaseCompleted;
        });

        const overallPercentage = overallTotal > 0 ? Math.round((overallCompleted / overallTotal) * 100) : 0;
        const overallElement = document.getElementById('overallPercent');
        
        if (overallElement) {
            overallElement.textContent = `${overallPercentage}%`;
        }
        
        console.log(`Overall: ${overallCompleted.toFixed(2)}/${overallTotal} (${overallPercentage}%)`);
    }

    // Calculate the weight of a task
    function calculateTaskWeight(taskId) {
        if (!taskHierarchy[taskId]) return 1;
        
        let weight = 1;
        const children = taskHierarchy[taskId].children;
        
        children.forEach(childId => {
            weight += calculateTaskWeight(childId);
        });
        
        return weight;
    }

    // Calculate completion ratio for a task
    function calculateTaskCompletion(taskId) {
        const checkbox = document.querySelector(`input[data-task-id="${taskId}"]`);
        if (!checkbox) return 0;
        
        if (!taskHierarchy[taskId] || taskHierarchy[taskId].children.length === 0) {
            return checkbox.checked ? 1 : 0;
        }
        
        const children = taskHierarchy[taskId].children;
        let childrenCompleted = 0;
        let totalChildren = children.length;
        
        children.forEach(childId => {
            childrenCompleted += calculateTaskCompletion(childId);
        });
        
        return totalChildren > 0 ? childrenCompleted / totalChildren : 0;
    }

    // Update task row styling based on completion
    function updateTaskRow(checkbox) {
        const row = checkbox.closest('tr');
        const taskId = checkbox.getAttribute('data-task-id');
        
        if (checkbox.checked) {
            row.classList.add('completed');
            row.classList.remove('overdue', 'due-soon');
            
            const completeDateInput = row.querySelector('td:nth-child(7) input[type="date"]');
            if (completeDateInput && !completeDateInput.value) {
                completeDateInput.value = new Date().toISOString().split('T')[0];
            }
        } else if (checkbox.indeterminate) {
            row.classList.remove('completed');
            
            const completeDateInput = row.querySelector('td:nth-child(7) input[type="date"]');
            if (completeDateInput) {
                completeDateInput.value = '';
            }
        } else {
            row.classList.remove('completed');
            
            const completeDateInput = row.querySelector('td:nth-child(7) input[type="date"]');
            if (completeDateInput) {
                completeDateInput.value = '';
            }
        }
    }

    // Auto-save functionality
    function autoSave() {
        const indicator = document.getElementById('autoSaveIndicator');
        indicator.textContent = 'Auto-saving...';
        indicator.style.color = '#5a6c7d';
        
        setTimeout(() => {
            saveProjectData();
            indicator.textContent = 'Auto-saved';
            setTimeout(() => {
                indicator.textContent = '';
            }, 2000);
        }, 500);
    }

    // Apply filters to the table
    function applyFilters() {
        const viewFilter = document.getElementById('viewFilter').value;
        const phaseFilter = document.getElementById('phaseFilter').value;
        const rows = document.querySelectorAll('#mainTable tbody tr');

        rows.forEach(row => {
            let showRow = true;

            if (row.classList.contains('phase-row') || row.classList.contains('section-row')) {
                return;
            }

            const checkbox = row.querySelector('input[type="checkbox"]');
            if (!checkbox) return;

            if (viewFilter === 'incomplete' && (checkbox.checked || checkbox.indeterminate)) {
                showRow = false;
            } else if (viewFilter === 'complete' && !checkbox.checked) {
                showRow = false;
            } else if (viewFilter === 'overdue') {
                const dueDate = row.querySelector('td:nth-child(6) input[type="date"]').value;
                const today = new Date().toISOString().split('T')[0];
                if (!dueDate || dueDate >= today || checkbox.checked) {
                    showRow = false;
                }
            } else if (viewFilter === 'due-soon') {
                const dueDate = row.querySelector('td:nth-child(6) input[type="date"]').value;
                const weekFromNow = new Date();
                weekFromNow.setDate(weekFromNow.getDate() + 7);
                const weekFromNowStr = weekFromNow.toISOString().split('T')[0];
                if (!dueDate || dueDate > weekFromNowStr || checkbox.checked) {
                    showRow = false;
                }
            }

            if (phaseFilter !== 'all') {
                const taskPhase = checkbox.getAttribute('data-phase');
                if (taskPhase !== phaseFilter) {
                    showRow = false;
                }
            }

            if (showRow && !row.style.display) {
                row.style.display = '';
            } else if (!showRow) {
                row.style.display = 'none';
            }
        });
    }

    // Export to Excel functionality
    function exportToExcel() {
        alert('Excel export functionality would be implemented here with SheetJS library');
    }

    // Save project functionality
    function saveProject() {
        saveProjectData();
        const dataStr = JSON.stringify(projectData, null, 2);
        const blob = new Blob([dataStr], { type: 'application/json' });
        const url = window.URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.setAttribute('hidden', '');
        a.setAttribute('href', url);
        a.setAttribute('download', generateFilename('json'));
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
        
        const indicator = document.getElementById('autoSaveIndicator');
        if (isIOS()) {
            indicator.textContent = 'Saved! Check Files app or Downloads';
            indicator.style.color = '#007AFF';
        } else {
            indicator.textContent = 'Project Saved!';
            indicator.style.color = '#2c3e50';
        }
        
        setTimeout(() => {
            indicator.textContent = '';
        }, 4000);
    }

    // Load project
    function loadProject() {
        const fileInput = document.getElementById('projectFileInput');
        
        if (isIOS()) {
            const iosGuidance = 'On iOS: Browse to iCloud Drive, Google Drive, or Downloads folder to find your project files';
            setTimeout(() => {
                if (document.visibilityState === 'visible') {
                    const indicator = document.getElementById('autoSaveIndicator');
                    indicator.textContent = iosGuidance;
                    indicator.style.color = '#007AFF';
                    setTimeout(() => {
                        indicator.textContent = '';
                    }, 6000);
                }
            }, 1000);
        }
        
        fileInput.click();
    }

    // Handle file load
    function handleFileLoad(event) {
        const file = event.target.files[0];
        if (!file) return;

        const reader = new FileReader();
        reader.onload = function(e) {
            try {
                const loadedData = JSON.parse(e.target.result);
                
                if (loadedData && typeof loadedData === 'object') {
                    projectData = loadedData;
                    
                    if (loadedData.taskHierarchy) {
                        taskHierarchy = loadedData.taskHierarchy;
                    }
                    
                    loadProjectData();
                    
                    const indicator = document.getElementById('autoSaveIndicator');
                    indicator.textContent = '✅ Enhanced Project Loaded Successfully!';
                    indicator.style.color = isIOS() ? '#007AFF' : '#2c3e50';
                    setTimeout(() => {
                        indicator.textContent = '';
                    }, 3000);
                } else {
                    throw new Error('Invalid file format');
                }
            } catch (error) {
                const message = isIOS() ? 
                    'Please select a valid AV Project .json file from your Files app' :
                    'Error loading project file. Please ensure it\'s a valid AV Project file.';
                alert(message);
                console.error('Error loading project:', error);
            }
        };
        
        reader.readAsText(file);
        event.target.value = '';
    }

    // Generate filename
    function generateFilename(extension = 'json') {
        const projectName = document.getElementById('projectName').value || 'AV_Project';
        const now = new Date();
        
        const month = String(now.getMonth() + 1).padStart(2, '0');
        const day = String(now.getDate()).padStart(2, '0');
        const hours = String(now.getHours()).padStart(2, '0');
        const minutes = String(now.getMinutes()).padStart(2, '0');
        
        const cleanProjectName = projectName.replace(/[<>:"/\\|?*]/g, '_').replace(/\s+/g, '_');
        
        return `${cleanProjectName}_${month}.${day}_${hours}${minutes}.${extension}`;
    }

    // Save project data
    function saveProjectData() {
        const data = {
            projectName: document.getElementById('projectName').value,
            projectManager: document.getElementById('projectManager').value,
            lastSaved: new Date().toISOString(),
            version: '2.0-hierarchical-pwa',
            taskHierarchy: taskHierarchy,
            tasks: []
        };

        const rows = document.querySelectorAll('#mainTable tbody tr[data-task-id]');
        rows.forEach((row, index) => {
            const cells = row.querySelectorAll('td');
            if (cells.length >= 8) {
                const checkbox = cells[0].querySelector('input[type="checkbox"]');
                const taskId = row.getAttribute('data-task-id');
                const parentId = row.getAttribute('data-parent-id');
                const phase = row.getAttribute('data-phase');
                const task = cells[2].textContent.replace(/▶|▼/g, '').trim();
                const assignedTo = cells[3].querySelector('input').value;
                const startDate = cells[4].querySelector('input').value;
                const dueDate = cells[5].querySelector('input').value;
                const completeDate = cells[6].querySelector('input').value;
                const notes = cells[7].querySelector('textarea').value;

                data.tasks.push({
                    id: taskId,
                    parentId: parentId,
                    phase: phase,
                    completed: checkbox ? checkbox.checked : false,
                    indeterminate: checkbox ? checkbox.indeterminate : false,
                    task: task,
                    assignedTo: assignedTo,
                    startDate: startDate,
                    dueDate: dueDate,
                    completeDate: completeDate,
                    notes: notes,
                    level: row.classList.contains('task-level-2') ? 2 : 
                           row.classList.contains('task-level-3') ? 3 :
                           row.classList.contains('task-level-4') ? 4 : 1
                });
            }
        });

        projectData = data;
    }

    // Load project data
    function loadProjectData() {
        if (projectData && projectData.projectName) {
            document.getElementById('projectName').value = projectData.projectName;
            document.getElementById('projectManager').value = projectData.projectManager || '';

            if (projectData.taskHierarchy) {
                taskHierarchy = projectData.taskHierarchy;
            }

            if (projectData.tasks) {
                projectData.tasks.forEach(taskData => {
                    const row = document.querySelector(`tr[data-task-id="${taskData.id}"]`);
                    if (row) {
                        const cells = row.querySelectorAll('td');
                        
                        const checkbox = cells[0].querySelector('input[type="checkbox"]');
                        if (checkbox) {
                            checkbox.checked = taskData.completed || false;
                            checkbox.indeterminate = taskData.indeterminate || false;
                            updateTaskRow(checkbox);
                        }
                        
                        const assignedInput = cells[3].querySelector('input');
                        const startDateInput = cells[4].querySelector('input');
                        const dueDateInput = cells[5].querySelector('input');
                        const completeDateInput = cells[6].querySelector('input');
                        const notesTextarea = cells[7].querySelector('textarea');
                        
                        if (assignedInput) assignedInput.value = taskData.assignedTo || '';
                        if (startDateInput) startDateInput.value = taskData.startDate || '';
                        if (dueDateInput) dueDateInput.value = taskData.dueDate || '';
                        if (completeDateInput) completeDateInput.value = taskData.completeDate || '';
                        if (notesTextarea) notesTextarea.value = taskData.notes || '';
                    }
                });
            }

            Object.keys(taskHierarchy).forEach(taskId => {
                if (taskHierarchy[taskId].children.length > 0) {
                    updateParentTask(taskId);
                }
            });

            updateProgress();
        }
    }

    // Add custom task
    function addCustomTask() {
        const phase = prompt('Which phase should this task belong to? (1-5)');
        if (phase && phase >= 1 && phase <= 5) {
            const taskName = prompt('Enter task name:');
            if (taskName) {
                const tbody = document.querySelector('#mainTable tbody');
                const newTaskId = `custom-${Date.now()}`;
                
                const newRow = document.createElement('tr');
                newRow.className = 'task-level-1';
                newRow.setAttribute('data-task-id', newTaskId);
                newRow.setAttribute('data-phase', phase);
                
                newRow.innerHTML = `
                    <td><input type="checkbox" data-phase="${phase}" data-task-id="${newTaskId}"></td>
                    <td>
                        <button class="btn btn-small btn-add" onclick="addSubTask('${newTaskId}')" title="Add Sub-task">+</button>
                        <button class="btn btn-small btn-remove" onclick="removeTask('${newTaskId}')" title="Remove Task">×</button>
                    </td>
                    <td>
                        <span class="expand-indicator" onclick="toggleSubTasks('${newTaskId}')" style="visibility: hidden;">▶</span>
                        ${taskName}
                        <span class="parent-completion" id="completion-${newTaskId}"></span>
                    </td>
                    <td><input type="text" placeholder="Assigned to"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><input type="date"></td>
                    <td><textarea placeholder="Notes"></textarea></td>
                `;
                tbody.appendChild(newRow);
                
                taskHierarchy[newTaskId] = {
                    id: newTaskId,
                    parentId: null,
                    phase: phase,
                    children: [],
                    isExpanded: true
                };
                
                setupCheckboxListeners();
                
                const inputs = newRow.querySelectorAll('input:not([type="checkbox"]), textarea');
                inputs.forEach(input => {
                    input.addEventListener('input', autoSave);
                    
                    if (isIOS()) {
                        input.addEventListener('focus', function() {
                            this.style.fontSize = '16px';
                        });
                        input.addEventListener('blur', function() {
                            this.style.fontSize = '';
                        });
                    }
                });

                updateProgress();
            }
        }
    }
</script>

</body>
</html>
